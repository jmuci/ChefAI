Add/modify Room schema with:

- @Entity + @PrimaryKey
- TypeConverters if needed
- DAO with suspend functions & Flow
- Repository interface + impl
- Hilt module to provide DAO + DB
- Migration if schema changes
- Test for DAO using in-memory DB
